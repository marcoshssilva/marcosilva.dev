kind: pipeline
spec:
  stages:
  - type: ci
    spec:
      steps:
      - name: 'download-dependencies'
        type: run
        spec:
          container: node:16
          script: |-
            npm install --force
